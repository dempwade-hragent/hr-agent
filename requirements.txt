# Flask Backend (Async version for Agents SDK)
Quart==0.19.4
quart-cors==0.7.0

# Production server
hypercorn==0.16.0

# Data Processing
pandas>=2.2.0

# PDF Generation
reportlab==4.0.7

# OpenAI Agents SDK (NEW! 2025 version)
# Note: The actual package name may vary - check PyPI for the correct package
# This is the new Agents SDK, NOT the old openai package
openai>=1.12.0
openai-agents>=0.4.0
pydantic>=2.6
typing-extensions>=4.9
# If there's a separate agents-sdk package, use that instead
# agents-sdk>=1.0.0
